# Skim stores the OpenAI API key in the extension Options page (chrome.storage.local).
# You do not need a .env file for the default build.
#
# Optional: if you add local tooling or scripts later, you might use:
# OPENAI_API_KEY=sk-...
