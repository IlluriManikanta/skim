{
  "name": "skim",
  "version": "0.1.0",
  "private": true,
  "description": "Source-grounded reading assistant browser extension",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "pdfjs-dist": "^4.10.38"
  }
}
